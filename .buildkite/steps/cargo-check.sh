#!/usr/bin/env bash
# cargo-check.sh — cargo check --workspace (fast compile guard).
# v1 placeholder: real implementation runs cargo check.
set -euo pipefail

echo "--- [cargo-check] starting"
echo "[cargo-check] placeholder — no real cargo check yet"
echo "[cargo-check] ok"
