[
  {
    "question_type": "multiple_choice",
    "prompt": "The project goal says main-thread CPU, but the process sits at 25-90% CPU overall. The design's `consumeLines` receive-buffer-scan finding (278 top-of-stack samples) and its engine JSON-decode finding (~2114 samples on the EngineClient queue) are both real but both run off the main thread, so entries 12 and 13 are currently tagged deferred. Which is the actual target?",
    "choices": [
      "Main-thread CPU only — keep entries 12 and 13 deferred (recommended)",
      "Total process CPU — promote entries 12 and 13 to in-scope",
      "Main-thread first, then reassess off-main after the verification sweep"
    ],
    "anchor": "risks--open-questions"
  },
  {
    "question_type": "yes_no",
    "prompt": "The ~1.0-1.1 GB footprint is named in the project description but absent from the goal, and nothing in this plan addresses it. Should memory footprint be added to this project's scope?",
    "anchor": "risks--open-questions"
  }
]
