\* Does transient_recovery's unguarded same-kind resume path
\* (transient_recovery.rs:444 -> work/executions_runs.rs:184-233) rescue
\* the liveness property the churn guard breaks?
SPECIFICATION Spec

CONSTANTS
    MaxExecs               = 6
    MaxImpls               = 3
    MaxTime                = 1
    MaxDeaths              = 3
    ChurnWindow            = 10
    ChurnThreshold         = 3
    TransientResumeEnabled = TRUE
    DeathStatuses          = {"orphaned", "failed", "cancelled", "abandoned"}
    KindScopedCount        = FALSE

INVARIANTS
    GuardParksOnlyOnReviewChurn

PROPERTIES
    ReviewEventuallyLands

\* Quiescence is legitimate here: when every execution is terminal and
\* the model bounds are exhausted, no action is enabled. That is not a
\* liveness bug, it is the end of the modelled behaviour.
CHECK_DEADLOCK FALSE
