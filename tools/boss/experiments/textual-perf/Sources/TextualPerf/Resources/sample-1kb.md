# Baseline Sample

A short markdown document used as a baseline to measure Textual's
per-view overhead independent of document size.

## Section

A paragraph with **bold**, *italic*, and `inline code` spans.

- Item one
- Item two
- Item three

> A blockquote with a short sentence inside.

```swift
let x = 42
print(x)
```

| Column A | Column B |
| -------- | -------- |
| one      | two      |
| three    | four     |

Another paragraph at the end.
