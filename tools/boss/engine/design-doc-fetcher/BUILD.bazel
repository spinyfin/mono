load("@mono_crates//:defs.bzl", "all_crate_deps")
load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

exports_files(["Cargo.toml"])

rust_library(
    name = "design-doc-fetcher",
    srcs = glob(["src/**/*.rs"]),
    crate_name = "boss_design_doc_fetcher",
    crate_root = "src/lib.rs",
    edition = "2024",
    proc_macro_deps = all_crate_deps(proc_macro = True),
    visibility = [
        "//tools/boss/engine/core:__pkg__",
    ],
    deps = [
        "//lib/rust/git_utils",
        "//tools/boss/github",
    ] + all_crate_deps(normal = True),
)

rust_test(
    name = "design-doc-fetcher_test",
    size = "small",
    crate = ":design-doc-fetcher",
    edition = "2024",
    proc_macro_deps = all_crate_deps(
        proc_macro = True,
        proc_macro_dev = True,
    ),
    deps = [
        "//lib/rust/git_utils",
        "//tools/boss/github",
    ] + all_crate_deps(
        normal = True,
        normal_dev = True,
    ),
)
