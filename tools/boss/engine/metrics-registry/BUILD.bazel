load("@mono_crates//:defs.bzl", "all_crate_deps")
load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

rust_library(
    name = "metrics_registry",
    srcs = glob(["src/**/*.rs"]),
    crate_name = "boss_engine_metrics_registry",
    crate_root = "src/lib.rs",
    edition = "2024",
    proc_macro_deps = all_crate_deps(proc_macro = True),
    visibility = [
        "//tools/boss/engine/core:__pkg__",
    ],
    deps = [
        "//tools/boss/engine/utils",
    ] + all_crate_deps(normal = True),
)

rust_test(
    name = "metrics_registry_test",
    size = "small",
    crate = ":metrics_registry",
    edition = "2024",
    proc_macro_deps = all_crate_deps(
        proc_macro = True,
        proc_macro_dev = True,
    ),
    deps = [
        "//tools/boss/engine/utils",
    ] + all_crate_deps(
        normal = True,
        normal_dev = True,
    ),
)
