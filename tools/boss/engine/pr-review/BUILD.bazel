load("@mono_crates//:defs.bzl", "all_crate_deps")
load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

rust_library(
    name = "pr-review",
    srcs = glob(["src/**/*.rs"]),
    crate_name = "boss_engine_pr_review",
    crate_root = "src/lib.rs",
    edition = "2024",
    proc_macro_deps = all_crate_deps(proc_macro = True),
    visibility = [
        "//tools/boss/engine/core:__pkg__",
    ],
    deps = [
        "//tools/boss/engine/prompt-fragments",
        "//tools/boss/engine/utils",
    ] + all_crate_deps(normal = True),
)

rust_test(
    name = "pr-review-test",
    size = "small",
    crate = ":pr-review",
    edition = "2024",
    proc_macro_deps = all_crate_deps(
        proc_macro = True,
        proc_macro_dev = True,
    ),
    deps = [
        "//tools/boss/engine/prompt-fragments",
        "//tools/boss/engine/utils",
    ] + all_crate_deps(
        normal = True,
        normal_dev = True,
    ),
)
