load("@mono_crates//:defs.bzl", "all_crate_deps")
load("@rules_rust//rust:defs.bzl", "rust_library")
load("//tools/boss/test-isolation:boss_rust_test.bzl", "boss_rust_test")

exports_files(["Cargo.toml"])

rust_library(
    name = "pr-review",
    srcs = glob(["src/**/*.rs"]),
    crate_name = "boss_pr_review",
    crate_root = "src/lib.rs",
    edition = "2024",
    proc_macro_deps = all_crate_deps(proc_macro = True),
    visibility = [
        "//tools/boss/engine/core:__pkg__",
        "//tools/boss/engine/driver:__pkg__",
        "//tools/boss/engine/proposal-validation:__pkg__",
    ],
    deps = [
        "//tools/boss/engine/structured-output",
        "//tools/boss/protocol",
    ] + all_crate_deps(normal = True),
)

boss_rust_test(
    name = "pr-review_test",
    size = "small",
    crate = ":pr-review",
    edition = "2024",
    proc_macro_deps = all_crate_deps(
        proc_macro = True,
        proc_macro_dev = True,
    ),
    deps = [
        "//tools/boss/engine/structured-output",
        "//tools/boss/protocol",
    ] + all_crate_deps(
        normal = True,
        normal_dev = True,
    ),
)
