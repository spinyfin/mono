load("@build_bazel_rules_apple//apple:macos.bzl", "macos_unit_test")
load("@build_bazel_rules_swift//swift:swift.bzl", "swift_library")

package(default_visibility = ["//visibility:private"])

macos_unit_test(
    name = "UpdateTests",
    size = "small",
    # xcodebuild owns DerivedData/result-bundle caches outside TEST_TMPDIR.
    env = {"MONO_TEST_ALLOW_OUTSIDE_WRITES": "1"},
    minimum_os_version = "15.0",
    deps = [":update_tests_lib"],
)

swift_library(
    name = "update_tests_lib",
    testonly = True,
    srcs = glob(["*.swift"]),
    copts = [],
    module_name = "UpdateTests",
    deps = ["//tools/boss/app-macos/Sources/UpdateCore"],
)
